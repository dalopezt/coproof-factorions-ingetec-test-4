% Label: Finite Verification

**Theorem (Finite Verification).** For every natural number $n \leq 2540160$, $n$ equals the sum of the factorials of its decimal digits if and only if $n \in \{1, 2, 145, 40585\}$.

More precisely, letting $F(n) = \sum_{i} d_i!$ where $d_0, d_1, \ldots$ are the decimal digits of $n$, we have:
$$\forall n \in \mathbb{N},\; n \leq 2540160 \implies \bigl(n = F(n) \iff n \in \{1, 2, 145, 40585\}\bigr).$$

*Proof.* Unsolved.